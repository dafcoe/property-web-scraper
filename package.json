{
  "name": "@dafcoe/property-web-scraper",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "rm -rf dist && tsc src/main.ts --noEmit && node build.ts"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "esbuild": "^0.23.1",
    "typescript": "^5.5.3"
  }
}
